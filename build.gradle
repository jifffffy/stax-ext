group 'org.jsun.staf'
version '1.0-SNAPSHOT'


allprojects {

    apply plugin: 'java'

    repositories {
        mavenLocal()
        mavenCentral()
    }

    tasks.withType(JavaCompile) {
        options.encoding = "UTF-8"
    }

}
